spring:
  application:
    name: studio
  datasource:
    url: jdbc:mysql://localhost:3306/studio_api?createDatabaseIfNotExist=true
    username: root
    password: MyPassword
  jpa:
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db.migration